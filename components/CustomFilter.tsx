import React from 'react'
import { ICustomFilterProps } from '@/types'

const CustomFilter = ({ title }: ICustomFilterProps ) => {
  return (
    <div>
      {title}
    </div>
  )
}

export default CustomFilter